# Core dependencies
asyncio-mqtt==0.16.2
aiohttp>=3.9.1
python-dotenv>=1.0.0

# Database
influxdb-client[async]>=1.38.0

# MQTT (pin to 1.x — asyncio-mqtt 0.16.2 is incompatible with paho-mqtt 2.x)
paho-mqtt>=1.6.1,<2.0.0

# Scheduling
schedule>=1.2.0

# HTTP requests
requests>=2.31.0

# Web framework
fastapi>=0.104.1,<1.0.0
uvicorn>=0.24.0,<1.0.0
python-multipart>=0.0.6
websockets>=12.0,<15.0

# Date/time handling
pytz==2023.3
astral==3.2  # For sunrise/sunset calculations

# Data processing
# Upper bounds pin the NumPy-2 / pandas-2 / scipy-1 major lines so a future
# `docker-compose build` (no lockfile) can't pull an incompatible major and
# silently break the optimizer/forecaster at runtime.
pandas>=2.2.0,<3.0.0
numpy>=2.0.0,<3.0.0
scipy>=1.13.0,<2.0.0

# MILP optimization — global-optimum battery schedule solver
pulp>=2.7.0,<4.0

# ML forecasting — autoregressive consumption model
# Optional: falls back to temperature-binned median if init fails.
# skforecast >=0.14 guarantees the `skforecast.recursive.ForecasterRecursive`
# import path used by ml_consumption_forecast (0.13 relocated it and breaks
# both import fallbacks). Server runs 0.22.
scikit-learn>=1.3.0,<2.0.0
skforecast>=0.14,<1.0

# Weather API specific
# openmeteo-py==0.0.1  # Commented out - will implement API calls directly

# Logging
colorlog==6.8.0

# Export formats (optional)
reportlab==4.0.7  # PDF generation
openpyxl==3.1.2   # Excel export

# Monitoring (optional)
prometheus-client==0.19.0

# Caching (optional)
# redis==5.0.1  # Uncomment if using Redis for distributed caching
# aiocache==0.12.2  # Uncomment for advanced caching features

# Development
pytest>=7.4.3
pytest-asyncio>=0.21.1
black>=23.12.0
flake8>=6.1.0
mypy>=1.7.1
pydantic>=2.5.2,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
types-requests>=2.31.0.10
types-pytz>=2023.3.1.1
